name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Debug Directory Structure
        run: |
          echo "Current directory: $(pwd)"
          ls -la
          echo "Bond-dashboard directory contents:"
          ls -la bond-dashboard || echo "Bond-dashboard directory not found"
      
      - name: Install Dependencies
        run: |
          if [ -d "bond-dashboard/frontend" ]; then
            cd bond-dashboard/frontend
            echo "Installing dependencies in frontend directory..."
            npm install
          else
            echo "Frontend directory not found in bond-dashboard. Checking bond-dashboard directory..."
            ls -la bond-dashboard
            echo "Installing dependencies in bond-dashboard directory..."
            cd bond-dashboard
            npm install
          fi
      
      - name: Build
        run: |
          if [ -d "bond-dashboard/frontend" ]; then
            cd bond-dashboard/frontend
            echo "Building in frontend directory..."
            npm run build
          else
            echo "Building in bond-dashboard directory..."
            cd bond-dashboard
            npm run build
          fi
      
      - name: List Build Output
        run: |
          if [ -d "bond-dashboard/frontend/build" ]; then
            echo "Frontend build directory contents:"
            ls -la bond-dashboard/frontend/build
          elif [ -d "bond-dashboard/build" ]; then
            echo "Bond-dashboard build directory contents:"
            ls -la bond-dashboard/build
          else
            echo "No build directory found!"
            ls -la bond-dashboard
          fi
      
      - name: Setup Pages
        uses: actions/configure-pages@v4
      
      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: './bond-dashboard/frontend/build'

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4 